import bgHeaderDesktop from '../../assets/images/bg-header-desktop.svg'

const Banner = () => {
    return(
        <header>
            <img src={ bgHeaderDesktop } alt="Banner" />
        </header>
    )
}

export default Banner